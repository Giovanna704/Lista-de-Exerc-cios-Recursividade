#include <stdio.h> 
// 2. Soma de números naturais: Escreva uma função recursiva em C que retorne a soma dos primeiros n números naturais.
int soma(int n){
if(n==1){
return 1;
}
else{
    return n+soma(n-1);
}
}
int main() {
int n=5;
printf("A soma do numero %d é: %d",n,soma(n));
}
